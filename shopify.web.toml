name = "react-router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npx drizzle-kit generate"
dev = "npx wrangler d1 migrations apply DB --local && npm exec react-router dev"
